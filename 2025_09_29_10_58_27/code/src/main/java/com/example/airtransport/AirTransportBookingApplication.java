package com.example.airtransport;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class to bootstrap the Air Transport Booking Spring Boot application.
 */
@SpringBootApplication
public class AirTransportBookingApplication {
    public static void main(String[] args) {
        SpringApplication.run(AirTransportBookingApplication.class, args);
    }
}
