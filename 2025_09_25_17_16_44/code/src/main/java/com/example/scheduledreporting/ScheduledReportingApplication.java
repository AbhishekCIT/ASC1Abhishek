package com.example.scheduledreporting;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Scheduled Reporting Spring Boot application.
 */
@SpringBootApplication
public class ScheduledReportingApplication {
    public static void main(String[] args) {
        SpringApplication.run(ScheduledReportingApplication.class, args);
    }
}
