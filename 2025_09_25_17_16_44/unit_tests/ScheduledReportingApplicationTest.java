package com.example.scheduledreporting;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test class for ScheduledReportingApplication.
 * Ensures that the Spring application context loads successfully.
 */
@SpringBootTest
public class ScheduledReportingApplicationTest {

    /**
     * Test that the application context loads without throwing exceptions.
     */
    @Test
    void contextLoads() {
        // Purpose: Verifies that the Spring Boot application context loads successfully.
        // No assertions needed; test will fail if context cannot be loaded.
    }
}
