package com.airtransport;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test to verify that the Spring Boot application context loads successfully.
 */
@SpringBootTest
public class AirTransportApplicationTest {
    /**
     * Purpose: Ensure the application context loads without exceptions.
     */
    @Test
    void contextLoads() {
        // If the context fails to load, this test will fail.
    }
}
