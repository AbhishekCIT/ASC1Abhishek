package com.example.airline;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test class for AirlineBookingApplication.
 * Ensures that the Spring application context loads successfully.
 */
@SpringBootTest
public class AirlineBookingApplicationTest {

    /**
     * Test to verify that the application context loads without exceptions.
     */
    @Test
    void contextLoads() {
        // The contextLoads test will fail if the application context cannot start.
    }
}
